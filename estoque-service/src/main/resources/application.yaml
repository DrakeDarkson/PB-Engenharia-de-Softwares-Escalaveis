spring:
  application:
    name: estoque-service
  datasource:
    url: jdbc:postgresql://localhost:5432/estoque-database
    username: postgres
    password: senha
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    exchange: stock.exchange
    queues:
      stockUpdatedQueue: stock.updated.queue
server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/